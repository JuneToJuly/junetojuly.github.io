REPLAY DEBUGGER MEDIA DROP FOLDER

Keep these exact filenames and replace the placeholder/current files as you capture better media.
The page will pick them up automatically after commit + push.

hero-replay.gif
  8–12 second hero loop: completed run -> open replay -> step events -> state/filesystem.

instrumentation.png
  Static screenshot of Instrumentation configuration. (Current screenshot already installed.)

replay-viewer.png
  Static fallback / overview of Replay Timeline + State + Filesystem. (Current screenshot installed.)

timeline.gif
  5–8 second loop: move backward/forward through timeline events.

state.gif
  5–8 second loop: select events and show historical state changing.

filesystem.gif
  5–8 second loop: step through CREATE/MODIFY/DELETE filesystem events.

execution-search.gif
  4–7 second loop: type a query and narrow recorded execution.

where-execution-went.gif
  5–8 second loop: show the Where Did Execution Go? navigation across files.

full-workflow.mp4
  45–120 second walkthrough: configure -> execute -> replay -> inspect.
  Browser-friendly H.264 MP4 is recommended.

Capture tips:
- Prefer 16:9 or wide crops for GIFs/video.
- Record only the relevant VS Code area; avoid the whole desktop.
- Keep text readable at ~1200px page width.
- GIF loops should communicate one idea each.
